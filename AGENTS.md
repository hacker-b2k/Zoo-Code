# AGENTS.md

This file provides guidance to agents when working with code in this repository.

---

## 0. Start Here (MANDATORY)

```bash
git fetch origin
git switch integration
git pull
```

- **Canonical branch = `integration`** (not random `feat/*`).
- Full policy: [`docs/BRANCH_AND_UPSTREAM_POLICY.md`](docs/BRANCH_AND_UPSTREAM_POLICY.md), **ADR-005**.

### Forbidden: Deep Sequential Agentic Pipeline

Do **NOT** re-add or rebuild:

- `src/core/pipeline/**`
- `agenticMode` / `deepSequential`
- pipeline StageBar / pipeline approval UI

**Workers multi-agent is allowed** (`src/core/orchestration/**`, `spawn_worker`, etc.).
`new_task` (serial child) is allowed and is **not** Deep Sequential.

If a task asks to restore Deep Sequential, **refuse** unless maintainers accept a new ADR superseding ADR-005.

---

## Project Systems Overview

Before making ANY changes, be aware of these already-implemented systems:

### 1. Governance System

- **`WORKSPACE_GOVERNANCE.md`** — Absolute rules: no shortcuts, no cheating, plan first, no half-work, industry standard quality.
- **`PLANNING_REQUIRED.md`** — 5-stage pipeline: Research -> Design -> Plan -> Review -> Execute. Zero code without a plan.
- **`docs/ARCHITECTURE.md`** — Full architecture reference.
- **`docs/REQUIREMENTS.md`** — Standing requirements.
- **`docs/DEV_SETUP.md`** — Dev environment setup.
- **BRANCH STRATEGY**: Work on **`integration`**. Short-lived feature branches + PR **into `integration`**. Do not treat long-lived mixed `feat/*` bags as home base. Prefer not to push casually to `main`.

### 2. Upstream Sync System (Zoo-Code-Org/Zoo-Code)

This fork tracks upstream with **selective cherry-pick** (NOT merge):

- **`.upstream/STATE.json`** — Tracks **`lastFetchedCommit`** (last check) and **`lastSyncCommit`** (last successful import), plus imported/skipped lists. Committed to repo (lives on GitHub).
- **`.upstream/COMMITS.md`**, **`UPSTREAM_DECISIONS.md`**, **`IMPLEMENTATION_STATE.md`** — Human-readable logs.
- **`scripts/upstream-check.ps1`** — Fetch upstream, list new commits, update STATE.json.
- **`scripts/upstream-import.ps1 <sha>`** — Cherry-pick a specific commit, mark as IMPORTED.
- **`scripts/upstream-skip.ps1 <sha> "<reason>"`** — Mark commit as SKIPPED with reason.
- **Workflow**: `pnpm upstream:check` -> review -> `pnpm upstream:import <sha>` or `pnpm upstream:skip <sha> "<reason>"` -> **commit `.upstream/*` and push**.
- **Docs**: `docs/upstream-sync/README.md`, `docs/BRANCH_AND_UPSTREAM_POLICY.md`, ADR-004, ADR-005.
- **NEVER** `git merge upstream/main` — only selective cherry-pick.

### 3. ADR Decision Log

All architecture decisions documented in `docs/decisions/`:

- ADR-001: Workspace Governance
- ADR-002: Branch Strategy & PR Workflow
- ADR-003: Tool Selection & Development Stack
- ADR-004: Selective Upstream Sync System
- ADR-005: Integration Branch + Forbidden Systems (Deep Sequential ban)

---

## Code Guidance

- **Settings View Pattern**: When working on `SettingsView`, inputs must bind to the local `cachedState`, NOT the live `useExtensionState()`. The `cachedState` acts as a buffer for user edits, isolating them from the `ContextProxy` source-of-truth until the user explicitly clicks "Save". Wiring inputs directly to the live state causes race conditions.
- **Changesets**: Do NOT create `.changeset` files for each commit or code change. Changesets are managed separately by maintainers and should not be generated by agents during normal development.

## Test Placement Guidance

Prefer the narrowest test layer that proves the behavior. This follows standard test-pyramid guidance: keep most coverage in fast, focused tests; add integration tests for cross-module contracts; reserve end-to-end tests for full workflow confidence.

- Use package-local unit tests for pure logic, parsing, state transitions, validation, serialization, request construction, retry decisions, and error handling.
- Use integration tests when behavior depends on multiple internal modules working together, but does not require the real VS Code extension host or browser/webview runtime.
- Use `webview-ui` tests for React rendering, hooks, component state, forms, validation, and webview UI wiring.
- Use `apps/vscode-e2e` only when the behavior depends on the real VS Code extension host, VS Code workspace APIs, extension activation, webview/extension messaging, file watcher behavior, or a complete user workflow.
- Keep e2e tests focused on high-value smoke coverage across boundaries. Avoid placing detailed protocol, parsing, storage, retry, or edge-case assertions in e2e when they can be covered reliably at a lower layer.
- When fixing a regression, add the regression test at the lowest layer that would have failed for the bug. Add an e2e test only if lower-level tests cannot represent the failure mode.
